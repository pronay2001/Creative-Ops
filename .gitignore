# Dependencies
node_modules/

# Environment secrets
.env
.env.local
.env.*.local

# Uploads (local disk files — keep directory, ignore contents)
uploads/*
!uploads/.gitkeep

# Replit internals
.local/
.replit
replit.nix
*.nix
zipFile.zip

# OS
.DS_Store
Thumbs.db
